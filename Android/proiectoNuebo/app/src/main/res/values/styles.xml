<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema general de la aplicación -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Personaliza tu tema general aquí -->
    </style>

    <!-- Estilo personalizado para el EditText -->
    <style name="CustomEditTextStyle" parent="Widget.AppCompat.EditText">
        <!-- Color del texto -->
        <item name="android:textColor">#000000</item>
        <!-- Tamaño del texto -->
        <item name="android:textSize">16sp</item>
        <!-- Fuente personalizada -->
        <item name="android:fontFamily">@font/quicksand_bold</item>
        <!-- Fondo personalizado -->
        <item name="android:background">@drawable/custom_edittext_background</item>
        <!-- Padding interior -->
        <item name="android:padding">10dp</item>
        <!-- Color del hint (texto de ayuda) -->
        <item name="android:textColorHint">#888888</item>
        <!-- Otros atributos personalizados -->
        <!-- Añade más atributos según tus necesidades -->
    </style>
    <style name="RoundedButtonStyle">
        <item name="android:background">@drawable/rounded_button_background</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">8dp</item>
        <!-- Otros atributos que desees personalizar -->
    </style>
</resources>
